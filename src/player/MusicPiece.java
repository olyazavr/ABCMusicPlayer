package player;

/**
 * Interface that will help build all of the ADT components. Everything will be
 * immutable.
 * 
 * MusicPiece = Signature(title: string, composer: string, length: float meter:
 * string, tempo: string, key: string) + Voice(name: string, measures:
 * ArrayList<Measure(symbols: List<MusicSymbol>, syllables: List<Syllable>)>)
 * 
 * 
 */
public interface MusicPiece {
    // FIXME: add methods lol
}
